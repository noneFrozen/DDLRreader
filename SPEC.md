# DDL Radar 产品与系统设计规约

状态：待用户书面复核  
日期：2026-08-12  
项目类型：AI4SE 期末项目 B（非 harness 应用类项目）

## 1. 问题陈述

大学生通常同时面对课程作业、展示、实验报告和项目交付。普通待办工具只能记录截止日期，不能回答三个更重要的问题：现有可用时间是否足够、冲突会在哪一天出现、今天应该先做什么。

DDL Radar 是面向大学生的个人截止日期冲突与工作量规划网页应用。用户录入任务、剩余工时、截止时间和未来可用时间后，系统用确定性算法识别容量缺口，生成未来 7–14 天可执行计划，并在进度变化后重新规划。

30 秒产品说明：**DDL Radar 不只是提醒你什么时候截止，而是提前算出时间够不够，并把剩余工作排进每天真正可用的时间里。**

## 2. 目标与非目标

### 2.1 MVP 目标

- 让用户在一个核心流程中完成“设置可用时间 → 录入任务 → 查看冲突 → 生成计划”。
- 对截止日前累计工作量与累计容量进行可解释的风险判断。
- 生成稳定、可复现、可手工调整的 7–14 天日程。
- 用户报告进度或错过安排后，保留已完成记录并重新规划剩余工作。
- 提供 ICS 导出、Docker 分发、一键测试和 GitLab CI。

### 2.2 非目标

- MVP 不包含多人协作、教师端、社交排名或校园教务系统爬取。
- MVP 不包含自主调用工具、多轮决策的智能体。
- MVP 不调用 LLM 或付费 API；核心算法必须离线、确定性运行。
- MVP 不自动修改用户的第三方日历，只提供 ICS 文件导出。

## 3. 目标用户与用户故事

主要用户是同时修读多门课程、需要自行安排课外时间的大学生。

1. 作为学生，我希望录入课程任务、截止时间和预计剩余工时，以便系统理解我的真实工作量。
2. 作为学生，我希望设置每周规律可用时间和某天的临时变化，以便计划避开上课、睡眠与不可用时段。
3. 作为学生，我希望看到具体哪一个截止日前会缺少多少小时，以便尽早降低范围、增加时间或调整优先级。
4. 作为学生，我希望一键生成未来 7–14 天的工作块，以便知道今天应该做什么。
5. 作为学生，我希望标记完成量或错过的安排并重新规划，以便计划能跟随现实变化。
6. 作为学生，我希望手动移动或锁定某个工作块，以便保留个人决定而不被自动规划覆盖。
7. 作为学生，我希望导出 ICS 文件，以便把最终计划加入常用日历。

## 4. 核心流程与界面

### 4.1 核心流程

1. **可用时间**：设置时区、规划范围、每周规律可用时段和日期例外。
2. **任务录入**：录入课程、任务、截止时间、剩余工时、优先级、依赖与是否可拆分。
3. **冲突分析**：显示总体风险、最早容量缺口、缺口小时数和造成冲突的任务。
4. **生成计划**：生成时间块；用户可锁定、移动、完成或跳过，并触发重新规划。

### 4.2 已选视觉方向

采用 Open Design 生成并由用户确认的 **B「平衡型 / Organic Productive」**，使用 `frontend-design` skill。设计系统为项目自定义的 **DDL Radar Biomimetic Organic — Balanced**，不直接套用第三方产品设计系统。

视觉规则：

- 浅灰绿雾面背景、暖象牙色半透明工作面板、深植物绿主要操作。
- 色彩基准：背景 `#E8EDDF`、表面 `#F7F8EE`、浅绿 `#D8E0CF`、中绿 `#65795B`、深绿 `#40543A`、正文 `#3E493B`。
- 18–22px 有机圆角、细灰绿边框、柔和漫射阴影；不使用通用企业蓝、紫色渐变或装饰性植物插画。
- 桌面端采用三栏工作区：左侧流程、中间冲突与日程、右侧任务和摘要；窄屏按流程 → 结论 → 任务 → 日程顺序堆叠。
- 标题保留编辑感，正文与数据使用清晰的界面字体；信息密度高于“视觉型”，但仍保留充足间距。
- 高风险、时间紧张、可安排分别使用低饱和砖红、赭黄、植物绿，并同时显示图标和文字。
- 所有交互元素提供 hover、active、disabled 与清晰的 `:focus-visible` 状态。

### 4.3 关键页面状态

- 首次使用：解释四步流程，并提供三条示例任务的可选导入。
- 数据不足：明确指出缺少截止时间、剩余工时或可用时间，不生成伪精确结果。
- 无冲突：显示“当前容量可覆盖任务”，仍保留 10% 缓冲。
- 有冲突：突出最早冲突日期、缺口量和相关任务，并提供返回修改入口。
- 无法排程：说明具体原因，例如“2 小时不可拆分任务找不到连续 2 小时空档”。
- 重新规划：已完成与已锁定时间块保持不变，仅调整未开始部分。

## 5. 功能规约

### 5.1 课程与任务管理模块

**输入**：课程名称/颜色；任务名称、截止时间、剩余工时、优先级、最小时间块、是否可拆分、依赖任务。  
**行为**：验证字段，统一换算为 30 分钟整数块；保存任务；依赖关系必须无环。  
**输出**：可编辑任务列表及总剩余工时。  
**边界条件**：剩余工时为 0 的任务视为完成；截止时间早于当前时间的未完成任务标记为逾期；不可拆分任务的最小块等于完整剩余工时。  
**错误处理**：字段旁显示可执行错误信息；循环依赖被拒绝并指出涉及任务。

### 5.2 可用时间模块

**输入**：浏览器时区、规划天数、每周规律时段、日期例外、休息间隔。  
**行为**：将时段离散为 30 分钟块；例外规则覆盖规律规则；重叠时段合并。  
**输出**：每日容量和未来 7–14 天可用时间轴。  
**边界条件**：跨午夜时段拆为两个日期片段；已经过去的时间块不参与新计划。  
**错误处理**：结束时间不晚于开始时间时拒绝保存；无可用时间时允许分析但禁止生成空计划。

### 5.3 冲突分析模块

**输入**：未完成任务、依赖关系、截止时间、剩余工时、有效可用时间、10% 缓冲。  
**行为**：按每个截止节点累计必须完成的工作量，并与该节点前累计可用容量比较。容量先扣除 10% 缓冲。依赖尚未满足的任务不能在前置任务之前占用时间。  
**输出**：红/黄/绿总体风险、最早冲突节点、容量缺口、受影响任务和解释文本。`firstConflict.taskIds` 必须包含截止时间不晚于首个冲突节点、且仍有未完成工作量的全部累计贡献任务；按截止时间、优先级、创建时间和任务 ID 稳定排序，不能只返回该节点新到期的任务。
**风险定义**：

- 红色高风险：任一截止节点的累计工作量大于缓冲后累计容量。
- 黄色时间紧张：没有缺口，但最小剩余裕量小于计划范围容量的 10%，或存在不可拆分任务连续空档风险。
- 绿色可安排：所有截止节点均有至少 10% 容量裕量，且不可拆分任务均存在可用连续空档。

**边界条件**：同一时刻截止的任务作为同一节点计算；已经逾期且未完成的任务直接为红色。  
**错误处理**：输入不完整时返回结构化“不可分析”结果，不以 0 代替缺失数据。

### 5.4 计划生成与重新规划模块

**输入**：分析通过或允许带风险继续的任务集、可用块、用户锁定块。  
**行为**：以 30 分钟为单位分配。排序依次采用最小松弛时间、最早截止时间、较高优先级、创建时间，保证相同输入得到相同输出。先满足依赖，再安排不可拆分任务，最后填充分拆任务。  
**输出**：7–14 天 `ScheduleBlock` 列表、未安排工作量和每个分配决定的简短解释。  
**重新规划**：冻结已完成、已开始和用户锁定块；从当前时间开始重新分配剩余工作。  
**边界条件**：用户可带着红色风险生成“尽力计划”，但界面必须保留未安排小时数和风险说明。  
**错误处理**：算法不能静默丢弃工作量；所有未安排工时必须归入具体任务。

### 5.5 进度与手工调整模块

**输入**：完成的分钟数、块状态、拖动后的起止时间、锁定状态。  
**行为**：更新任务剩余工时；移动操作不得与不可用时间或其他锁定块重叠；用户确认后触发重新分析。  
**输出**：更新后的任务、风险和计划版本。  
**边界条件**：完成量超过剩余量时按剩余量结算并提示；移动到截止时间后需要二次确认。  
**错误处理**：冲突移动回滚，并显示冲突对象和可选空档。

### 5.6 ICS 导出模块

**输入**：当前有效计划版本。  
**行为**：为每个计划块生成稳定 UID，按用户时区输出 UTF-8 ICS；重新导出沿用相同块的 UID。  
**输出**：可下载 `.ics` 文件。  
**边界条件**：没有计划块时禁用导出；任务标题中的特殊字符正确转义。  
**错误处理**：导出失败不影响已保存计划，并给出重试入口。

### 5.8 统计看板模块

**输入**：当前用户的计划、任务、可用时间（服务端聚合）。  
**行为**：汇总任务概况（活跃数、已完成数、逾期数、剩余总工时、本周到期数、完成率）、优先级分布（高/中/低各维度任务数与剩余工时）、每日工作负载（已排工时 vs 可用容量）。全部通过 `GET /api/stats` 返回，纯函数聚合，不修改数据。  
**输出**：`StatsResponse` 包含 `taskSummary`、`priorityDistribution`、`dailyWorkload`。前端以 SVG 环形图与柱状图渲染，作为第五步导航。  
**边界条件**：无任务时环图显示占位；无计划时每日负载返回空数组；所有数据按 `user_id` 隔离。  
**错误处理**：参数错误返回 400；未登录返回 401。

### 5.9 日程交互增强模块

**输入**：用户选中的时间块（`ScheduleBlock`），包括拆分/合并/移动/锁定/状态变更/进度更新操作。  
**行为**：
- **拆分**：将时间块在同任务内按 30 分钟对齐点一分为二，保留原状态与锁定属性；至少 60 分钟块才可拆分。`POST /api/schedule-blocks/:id/split`
- **合并**：将相邻同任务同状态的时间块合并为一个；不同状态或锁定状态不一致的块拒绝合并。`POST /api/schedule-blocks/:id/merge`
- **拖拽**：在时间线上 pointer 拖拽移动时间块，松开后 PATCH 新起止时间。
- **键盘微调**：方向键上下移动 30 分钟，Shift+方向键移动 60 分钟。
- **撤销/重做**：通过 `Ctrl+Z`/`Ctrl+Shift+Z` 或工具栏按钮撤销/重做移动、锁定、状态、进度、拆分、合并操作，历史栈限制 20 条。

**输出**：更新后的时间块列表，前端实时反映。  
**边界条件**：拆分点必须对齐 30 分钟且不在边界上；合并仅允许相邻同任务时间块；已锁定块不可合并；拖拽/键盘移动后如冲突显示对方任务名。  
**错误处理**：不符合拆分/合并条件返回 409（`SPLIT_POINT_OUTSIDE`、`SPLIT_NOT_ALIGNED`、`SPLIT_TOO_SMALL`、`MERGE_NOT_SAME_TASK`、`MERGE_NOT_ADJACENT`、`MERGE_STATUS_INCOMPATIBLE`）；未找到块返回 404；未登录返回 401。

## 6. 系统架构

采用 TypeScript 单体仓库，按领域边界分层，而不是把业务规则写进 React 组件。

```text
React Web UI
    │ REST/JSON
Fastify API / Application Services
    ├── Task & Availability Service
    ├── Conflict Analysis Domain
    ├── Planning / Replanning Domain
    └── ICS Export Service
             │
        SQLite Repository
```

- `frontend`：React、TypeScript、Vite；负责四步流程、周视图和无障碍交互。
- `backend`：Fastify、TypeScript；负责输入验证、应用服务、持久化和文件导出。
- `domain`：无框架依赖的确定性分析与排程函数，可直接单元测试。
- `persistence`：SQLite；通过仓储接口隔离，测试使用临时数据库。
- `distribution`：多阶段 Docker 构建；一个容器提供 API 和构建后的静态 WebUI，SQLite 数据目录挂载为 volume。

数据流：用户输入 → API 验证 → 仓储保存 → 领域分析 → 计划生成 → 结果持久化 → UI 呈现/ICS 导出。

## 7. 数据模型

### User

- `id: UUID`
- `email: string`，唯一，存储时小写
- `passwordHash: string`，`salt:hash`（scrypt）
- `createdAt`, `updatedAt`

### Session

- `id: string`，随机 token
- `userId: UUID`
- `createdAt`, `expiresAt`

### Course

- `id: UUID`
- `name: string`，1–60 字符
- `color: string`，受限色板值
- `createdAt`, `updatedAt`

### Task

- `id`, `courseId?`, `title`
- `deadline: ISO datetime`
- `remainingMinutes: non-negative integer`
- `priority: low | medium | high`
- `splittable: boolean`
- `minimumBlockMinutes: positive multiple of 30`
- `status: active | completed | archived`
- `createdAt`, `updatedAt`

### TaskDependency

- `predecessorTaskId`, `successorTaskId`
- 复合唯一键；有向无环约束由领域服务校验

### AvailabilityRule

- `id`, `weekday`, `startLocalTime`, `endLocalTime`, `timezone`

### AvailabilityException

- `id`, `date`, `startLocalTime`, `endLocalTime`, `kind: available | unavailable`

### Plan

- `id`, `rangeStart`, `rangeEnd`, `version`, `riskLevel`, `createdAt`

### ScheduleBlock

- `id`, `planId`, `taskId`, `startAt`, `endAt`
- `status: planned | started | completed | skipped`
- `locked: boolean`
- 同一计划内时间不得重叠

## 8. API 边界

- `GET/POST/PATCH/DELETE /api/tasks`
- `GET/PUT /api/availability`
- `POST /api/analysis`：只分析，不写入计划
- `POST /api/plans`：生成新版本
- `POST /api/plans/:id/replan`
- `PATCH /api/schedule-blocks/:id`
- `GET /api/plans/:id/export.ics`

所有写操作使用 JSON Schema 验证。错误响应统一为 `{ code, message, fieldErrors?, details? }`，不得把堆栈返回给前端。

## 9. 非功能性需求

### 性能

- 在 200 个活动任务、14 天范围和 30 分钟粒度下，冲突分析与生成计划各自在普通笔记本上 500ms 内完成。
- 首屏生产构建压缩资源目标小于 500KB（不含 source map）。

### 安全与隐私

- MVP 不使用 LLM、付费 API 或第三方鉴权，因此没有用户 API key 录入流程。
- 部署平台令牌、registry 凭据等仅保存在 GitLab CI 受保护变量或部署平台密钥管理中，绝不进入仓库、日志或前端包。
- 所有输入在 API 边界验证；下载文件头使用固定安全文件名规则；不渲染用户输入 HTML。
- 多用户账号系统：邮箱 + 密码注册登录，密码用 Node 内置 `crypto.scrypt` 加盐哈希（`salt:hash`），比较用 `timingSafeEqual`；会话 token 随机生成并存入 SQLite，Cookie 为 `HttpOnly + SameSite=Lax`，7 天过期。登录失败不泄露邮箱是否存在。公网演示使用独立演示数据，不保存敏感个人信息；README 明确这一边界。

### 可用性与无障碍

- 键盘可以完成四步核心流程；焦点顺序与视觉顺序一致。
- 正文和控件满足 WCAG AA 对比度目标。
- 风险状态不只依赖颜色；所有表单错误与字段关联。
- 1440px 桌面宽度提供完整三栏布局，768–1023px 转为双栏，小于 768px 单栏。

### 可观测性

- 后端输出结构化日志：请求 ID、路由、状态码、耗时；不记录任务正文之外的敏感环境变量。
- 计划生成记录算法版本、输入摘要和未安排分钟数，便于复现问题。
- 提供 `/health` 健康检查。

### 可靠性

- 计划生成在数据库事务中写入；失败不覆盖上一个有效版本。
- 所有时间以 UTC 存储，以 IANA 时区解释和展示。

## 10. 技术选型与理由

- **TypeScript**：前后端共享类型和领域模型，适合课程规模与重构。
- **React + Vite**：适合精致单页交互，构建简单、生态成熟。
- **Fastify**：JSON Schema 验证与 TypeScript 支持良好，运行开销低。
- **SQLite**：个人规划工具数据量小、部署简单、适合 Docker volume 和确定性集成测试。
- **Vitest**：领域算法与前后端单元测试统一运行器。
- **Playwright**：验证四步核心流程和关键无障碍交互。
- **Docker**：满足全新机器冷启动与课程分发要求。
- **Open Design**：使用 `frontend-design` skill 创建原型；最终采用自定义 Biomimetic Organic — Balanced 设计系统。

## 11. 测试策略

严格执行红–绿–重构。核心测试不得依赖当前时间或随机顺序，统一注入时钟与稳定 ID。

- 冲突分析属性：增加工作量不能降低风险；减少可用时间不能降低风险；相同输入结果完全相同。
- 截止节点测试：容量刚好、10% 缓冲临界值、同一截止时间、逾期任务。
- 排程测试：依赖顺序、不可拆分块、锁定块、未安排工时守恒、重新规划不修改已完成块。
- API 测试：字段验证、错误结构、事务回滚、ICS 特殊字符与稳定 UID。
- UI 测试：四步流程、红色风险解释、A11y 键盘路径、生成与重新规划。
- 一键命令：`npm test`；CI 的 `unit-test` job 必须运行同一命令。

## 12. 分发与部署

- 本地：`docker build -t ddl-radar .` 后使用一条 `docker run` 命令启动，挂载 `/data` 保存 SQLite。
- CI：根目录 `.gitlab-ci.yml` 至少包含名为 `unit-test` 的 job，并增加容器构建验证。
- 公网：部署一个可访问的 WebUI 演示地址；演示数据与个人数据隔离。
- README 必须说明获取、运行、数据卷、无 API key、已知限制、目录结构和安全边界。

## 13. 验收标准

1. 新用户能在 5 分钟内完成三条任务和一周可用时间的录入。
2. 给定示例数据时，界面明确显示“周四前缺少 2.5 小时可用时间”。
3. 相同输入连续生成两次得到相同计划块顺序和时间。
4. 红色风险下生成尽力计划时，所有未安排分钟均可追溯到任务。
5. 锁定、已开始、已完成块在重新规划后保持不变。
6. ICS 可被主流日历导入，标题、时区和时间正确。
7. 200 个任务、14 天范围下分析与排程各自小于 500ms。
8. 桌面、平板和移动断点没有横向溢出；键盘可以完成核心流程。
9. `npm test` 一次运行所有核心测试并成功退出。
10. GitLab CI 包含 `unit-test` job，最后一次流水线通过。
11. Docker 镜像可在全新环境启动，健康检查和 WebUI 可访问。

## 14. 风险与明确决策

- **工时估计不准确**：这是产品固有限制；通过快速调整剩余工时和重新规划缓解，不声称预测用户真实效率。
- **贪心算法并非全局最优**：MVP 优先可解释性、稳定性和工程可测性；验收目标是满足约束与工作量守恒，不宣称数学最优。
- **时区和夏令时**：统一 UTC 存储并使用 IANA 时区库；跨 DST 的集成测试必须覆盖。
- **拖拽复杂度**：拖拽只是手工调整入口，键盘移动/编辑必须等价可用。
- **部署存储**：选择支持持久卷的容器平台；不在无持久磁盘环境中承诺数据保留。
- **身份范围**：升级为多用户，每个用户的数据按 `user_id` 隔离；不在实现阶段加入共享、角色权限或第三方 OAuth。
- **算法边界**：30 分钟粒度、10% 缓冲和稳定排序均为已确认规则，实现不得自行改成 AI 生成计划。

## 15. 已确认事项

- 产品：DDL Radar 大学生截止日期冲突与工作量规划工具。
- 平台：响应式网页应用。
- 原型范围：一个核心四步流程。
- 完成度：精致产品界面。
- 最终视觉方向：B「平衡型 / Organic Productive」。
- 核心算法：确定性 30 分钟块、10% 缓冲、累计容量冲突检测、稳定贪心排程。
- MVP 不含智能体或外部付费 API。
